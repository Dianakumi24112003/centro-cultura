import React from 'react';
import { Button } from 'react-bootstrap';

const Buttons = () => {
  return (
    <div>
      <Button variant="primary">Primer Boton</Button>
      <Button variant="secondary">Segundo Boton</Button>
      
    </div>
  );
}

export default Buttons;
